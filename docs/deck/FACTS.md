# FACTS

Every fact carries a source. Nothing here is estimated: each number was produced by a
command run against this repository, and the raw output is appended.

| Suite commit | Service commit | Date |
|---|---|---|
| `52c2fd5` | `8be07a2` | 7 September 2026 |

| ID | Fact | Source |
|---|---|---|
| F-01 | The suite and the service are separate Maven projects in separate git repositories: artifactId `booking-enrichment-test-suite` and `city-enrichment`. | `pom.xml:1-12` in each repo; `git rev-parse --show-toplevel` in both |
| F-02 | The suite declares no dependency on the service artifact. The only mention of `com.cozentus:city-enrichment` in the suite build file is a comment asserting the deliberate absence. | `pom.xml:21` - "DELIBERATE ABSENCE: there is no dependency on com.cozentus:city-enrichment" |
| F-03 | No test source file imports a service internal package (`matcher`, `enrich`, `bus`, `processor`, `service`). Count of such imports: 0. | `grep -rn 'import com.cozentus.enrichment.\(matcher\|enrich\|bus\|processor\|service\)' src/test/java | wc -l` -> 0 |
| F-04 | The suite launches the service as a separate operating-system process via `ProcessBuilder`, running `java -jar <service jar>`. The crossing is a real process boundary, not an in-JVM call. | `harness/ServiceController.java:114` (`javaBinary(), "-jar", jar.toString()`), `:125` (`new ProcessBuilder(command)`) |
| F-05 | The transport is a real Apache Kafka broker. The harness uses `KafkaProducer` and `KafkaConsumer` against a configured `bootstrap.servers`; there is no embedded or in-memory fake in the execution path. | `harness/KafkaServiceHarness.java:18,46,74` |
| F-06 | Topic names are configured as prefixes `booking.raw`, `booking.enriched`, `booking.flagged`. | `src/test/resources/config/test-local.properties:31-33` |
| F-07 | Under the `local` profile each scenario gets its own topics, suffixed with a scenario id (`booking.raw.<scenarioId>`), plus its own consumer group and its own service process. | `steps/Hooks.java` @Before; observed on the broker during execution as `booking.raw.<hex8>` triples |
| F-08 | Under `-Dsuite.env=external` the suite starts no process, creates and deletes no topic, and uses the configured topic names verbatim. | `harness/ServiceController.attach()`, `harness/KafkaServiceHarness`; verified by running the functional slice with the service jar absent from disk |
| F-09 | The reference city list holds 8 cities: Mumbai, New Delhi, Bangalore, Chennai, Kolkata, Pune, Hyderabad, Ahmedabad. | service `src/main/resources/cities.json` |
| F-10 | The matching contract in the service: confidence floor 0.85, short-name length threshold 6, max edit distance 1 for short names and 2 for long names; a candidate is accepted when it is within distance AND confidence >= the floor. | service `matcher/CityMatcher.java:21,24,25,26,66` |
| F-11 | The suite states no numeric threshold literal anywhere under `src/test`. `ThresholdGuard` fails the build if one appears; occurrences outside the guard's own source: 0. | `grep -rn '0\.85' src/test/ | grep -v ThresholdGuard | wc -l` -> 0; `support/ThresholdGuard.java` |
| F-12 | 7 feature files are committed; an eighth (`city_correction.feature`) is generated at build time from the CSV and is not committed. | `find src/test/resources/features -name '*.feature' | wc -l` -> 7; `data/FeatureGenerator.java`, bound at `process-test-classes` |
| F-13 | The built feature set declares 99 scenarios: 96 `Scenario:` plus 3 `Scenario Outline:`. These expand to 101 executed scenarios, because two outlines carry two example rows each. | `grep -rh '^\s*Scenario:' target/test-classes/features | wc -l` -> 96; `Scenario Outline:` -> 3; cucumber.json elements of type scenario -> 101 |
| F-14 | Per built feature file: city_correction 50, booking_enrichment 12, schema_contract 8, resilience 7, message_contract 6, edge_cases 6, routing_and_flagging 5, volume 5. | `grep -c '^\s*Scenario' target/test-classes/features/*/*.feature` |
| F-15 | The test data table `city-cases.csv` holds 50 rows and 9 columns: caseId, field, input, expected, outcome, reason, confidence, tags, note. | `src/test/resources/testdata/city-cases.csv`; `wc -l` minus header -> 50 |
| F-16 | Every city case is stated once for the origin field and once for the destination field; the field not under test is pinned to Mumbai. | `model/CityField.java`; `data/FeatureGenerator.given()`; `TestDataLoaderTest.everyCaseIsMirrored` |
| F-17 | 22 test classes contain 182 @Test methods, alongside 9 step-definition classes. | `find src/test/java -name '*Test.java' | wc -l` -> 22; `grep -rh '@Test' --include='*Test.java' | wc -l` -> 182 |
| F-18 | A full run executes 282 tests and 101 scenarios with 0 failures and 0 errors, in 4 minutes 9 seconds of Maven time. | `mvn -B clean test -Pkafka`, exit 0; output appended below |
| F-19 | The run produces six artefacts: cucumber.json, cucumber.xml, index.html, report-data.json, scenario-coverage.md and test-execution-summary-report.html. | `ls target/` after the run; listing appended below |
| F-20 | Tags actually present on built scenarios include @fast 88, @functional 73, @city-correction 58, @critical 44, @flagging 37, @high 36, @slow 15, @resilience 13, @routing 10, @contract 8, @schema 8, @smoke 7, @message-contract 7. | `grep -rhoE '@[a-z-]+' target/test-classes/features/*/*.feature | sort | uniq -c | sort -rn` |
| F-21 | Tag discipline is enforced by the build: every scenario needs exactly one type tag, exactly one priority tag, at least one area tag, a speed tag and exactly one @TC-nn. An environment axis (`@requires-service-config`) is enumerated and any unknown `@requires-` tag is rejected. | `support/TagDiscipline.java`; bound in `pom.xml` at process-test-classes |
| F-22 | Five guards run before the tests and fail the build: tag discipline, flag-reason coverage, planned-set alignment, traceability-matrix currency and the threshold-literal guard. Two run after: the execution guard and the build-result guard. | `pom.xml` exec executions; run output appended below |
| F-23 | The planned catalogue `spec/test-cases.yaml` holds 99 cases and is reconciled against the feature files by `PlannedSetGuard`, which fails the build on any difference. | run output: "Planned set aligned: 99 cases in spec/test-cases.yaml and in the features." |
| F-24 | A run that selects no scenario fails the build. `mvn test -Pkafka -Dcucumber.filter.tags="@TC-9999"` exits 1 with "the run reported success having executed NO scenarios". | `support/ExecutionGuard.java`; command executed, exit 1 |
| F-25 | Schema conformance is implemented: three JSON schemas are validated against real messages using the networknt json-schema-validator. | `src/test/resources/contracts/*.schema.json`; `contract/SchemaValidator.java`; `schema_contract.feature` |
| F-26 | A mutation mechanism exists and is executable: a contract stub replays fixtures in place of the service, and 15 fixtures each break one rule. In the last full run the baseline was green over 99 cases and all 15 fixtures turned exactly their expected scenarios red. | `run-mutation-check.sh`; `src/main/resources/stub/*.json` (15 fixtures + plan); `docs/MUTATION_CHECK.md`, generated from the runs |
| F-27 | The generated report states 100% critical pass rate (44/44), 100% overall (99/99), 9 of 9 exit criteria met, and recommends "Proceed with conditions" because DEF-111 is open. | `target/report-data.json` after the run; `report/ExitCriteria.java` |
| F-28 | Traceability runs from case to clause to result: `TraceabilityMatrix` generates `docs/traceability-matrix.md` from the feature files joined to `docs/traceability-clauses.tsv`, and fails the build if the committed copy has drifted. | `docs/TraceabilityMatrix.java`; run output: "Traceability matrix: current, 99 case(s)." |
| F-29 | CI runs on push, pull_request, a 02:00 daily schedule and manual dispatch, in four jobs: unit (no broker), fast (`./run-tests.sh functional`), nightly (`./run-tests.sh nightly`) and summary-report. | `.github/workflows/test-suite.yml` |
| F-30 | The service under test is built in CI from its own repository using a read-only deploy key, not from a committed jar. | `.github/workflows/test-suite.yml`, step "Check out and build the service under test" |
| F-31 | With the broker stopped, a run aborts in 14 seconds with one statement of what is not ready, and makes 0 topic-creation retries. | measured: `docker stop dsv-kafka; bash run-tests.sh smoke` -> exit 1 in 14s; `grep -c 'Could not create topics'` -> 0 |
| F-32 | Five documented run profiles exist and each selects scenarios: smoke 7, functional 38, contract 8, full 95, nightly (volume tier). preflight runs 0 scenarios by design. | `run-tests.sh`; counts parsed from cucumber.json per profile |
