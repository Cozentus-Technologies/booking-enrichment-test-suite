# Traceability Matrix

Maps every `@TC-nn` scenario in `src/test/resources/features/` to the
specification clause it was derived from, its feature file, its priority
(`@critical`/`@high`/`@medium`/`@low`) and its type
(`@functional`/`@contract`/`@resilience`/`@volume`).

The TC list below was derived by scanning the feature files for `@TC-nn`
tags — the same catalogue `CoverageReporter` derives at report time — not
typed out by hand. Two spec documents are cited:

- **TEST_SUITE_SPEC.md**, this suite's own specification. Section 7.x is the
  scenario catalogue; each subsection below corresponds to one of its
  feature files.
- **SPEC.md**, the *service's* specification (owned by the service
  repository). Sections 4 (payload shapes), 5 (matching contract) and 6
  (flag reasons) are cited wherever a scenario asserts a rule that
  originates there. Not every scenario has a SPEC.md clause — TEST_SUITE_SPEC.md
  section 13 puts some suite behaviour (schema evolution, encoding/date
  round-tripping, oversized-payload handling) outside anything SPEC.md
  states explicitly; those rows say so rather than inventing a citation.

## `city_correction.feature` is generated — read this first

`city_correction.feature` is **not** committed; it is generated at build
time (`process-test-classes`, via `FeatureGenerator`) from
`src/test/resources/testdata/city-cases.csv`. TC-01 through TC-20 below
therefore trace to **two** places at once:

1. The CSV row with the matching `caseId` — the row is the source of the
   expected outcome, the tags, and the note explaining why the case exists.
2. SPEC.md section 5's matching contract and its sanity table, which is what
   every row's expected outcome is ultimately checked against.

Regenerate with `mvn process-test-classes`; do not hand-edit the
generated file (a build step overwrites it). A new case is added as a CSV
row, never as a change to this matrix or to `FeatureGenerator` itself.

---

<!-- GENERATED BELOW THIS LINE - see TraceabilityMatrix.java -->

Generated by `TraceabilityMatrix` from the feature files, the test data
table and `traceability-clauses.tsv`. Do not hand-edit: the next build
overwrites it, and `mvn test` fails if the committed copy has drifted.
To add a case, add the scenario and one line to the clause file.

## booking_enrichment.feature

12 case(s).

| Case | Scenario | Priority | Type | Derived from |
|---|---|---|---|---|
| TC-21 | Both cities resolve, so the booking is enriched with both corrected | critical | functional | §5 (matching, applied independently to both fields); §4 enriched booking shape |
| TC-22 | Origin resolves but destination fails, so the booking is flagged naming the destination | critical | functional | §2 whole-booking rule (either city fails ⇒ whole booking flagged); §6 `UNMATCHED_DESTINATION_CITY` |
| TC-23 | Both cities fail, producing two reasons ordered origin then destination | high | functional | §6 ("`reasons` is a list; both cities failing yields two entries, ordered origin then destination") |
| TC-24 | A valid city does not rescue a booking that has one invalid city | critical | functional | §2 whole-booking rule ("TMS never receives a half-enriched booking") |
| TC-25 | Enrichment metadata retains both original values alongside the corrected ones | high | functional | §4 enriched booking shape (`originalOrigin`, `originalDestination`); mutation table row 7 |
| TC-26 | Shipper, mode and requested date pass through unchanged | critical | functional | §4 ("All raw fields unchanged except origin/destination") |
| TC-27 | An unrecognised extra field does not break processing | medium | functional | §4 (raw booking shape; robustness to unspecified fields is implied, not asserted verbatim in SPEC.md) |
| TC-28 | A confidence score is present for each corrected city | medium | functional | §4 enriched booking shape (`originConfidence`, `destinationConfidence`); §5 step 4 (confidence definition) |
| TC-29 | Ambiguous input is flagged with candidates listed in reference order | high | functional | §5 step 5 (ambiguity, reference-file order); §6 `AMBIGUOUS_DESTINATION_CITY`; §5 sanity table `Delh` (list + `Delhi`) |
| TC-46 | Ambiguous origin is flagged with candidates in reference order | critical | functional | SPEC.md 5 step 5 (ambiguity, reference-file order); 6 `AMBIGUOUS_ORIGIN_CITY`. Origin mirror of TC-29; until it existed this reason was permitted by the contract and asserted by nothing. |
| TC-47 | Both fields ambiguous produces both reasons, origin then destination | high | functional | SPEC.md 6 (reason order, origin before destination). A consumer reading `reasons[0]` to route a booking for review gets the wrong field if the order is not fixed. |
| TC-48 | An exact match wins outright and is never reported ambiguous | critical | functional | SPEC.md 5 step 3 over step 4 (the exact rule outranks the fuzzy rule). Asserted through confidence exactly 1.0, since enriching to the right city with a lower confidence would mean the fuzzy rule guessed well, not that precedence held. |

## city_correction.feature

50 case(s).

| Case | Scenario | Priority | Type | Derived from |
|---|---|---|---|---|
| TC-01 | TC-01 - origin "Mumbai" is corrected to Mumbai | critical | functional | §5 step 3 (exact match) |
| TC-02 | TC-02 - origin "MUMBAI" is corrected to Mumbai | critical | functional | §5 step 2–3 (case-fold, exact match); §5 sanity table `MUMBAI` |
| TC-03 | TC-03 - origin "mumbai" is corrected to Mumbai | critical | functional | §5 step 2–3 (case-fold, exact match) |
| TC-04 | TC-04 - origin "··Mumbai··" is corrected to Mumbai | high | functional | §5 step 2 (trim); §5 sanity table `  New   Delhi ` |
| TC-05 | TC-05 - origin "New···Delhi" is corrected to New Delhi | high | functional | §5 step 2 (collapse internal whitespace) |
| TC-06 | TC-06 - origin "Mumbi" is corrected to Mumbai | critical | functional | §5 step 4 (fuzzy, d=1, name length > 6 ⇒ cap 2); §5 sanity table `Mumbi` |
| TC-07 | TC-07 - origin "now·delhi" is corrected to New Delhi | critical | functional | §5 step 4 (token matching via `delhi`); §5 sanity table `now delhi` |
| TC-08 | TC-08 - origin "Bangalor" is corrected to Bangalore | critical | functional | §5 step 4 (fuzzy, d=1); §5 sanity table `Bangalor` |
| TC-09 | TC-09 - origin "kolkatta" is corrected to Kolkata | high | functional | §5 step 4 (fuzzy, d=1, doubled letter) |
| TC-10 | TC-10 - origin "Ahmedabd" is corrected to Ahmedabad | high | functional | §5 step 4 (fuzzy, d=1, deletion) |
| TC-11 | TC-11 - origin "Pne" is corrected to Pune | critical | functional | §5 step 4 (short name, length ≤ 6 ⇒ cap 1, JW ≥ 0.85); §5 sanity table `Pne`; boundary pair with TC-13 |
| TC-12 | TC-12 - origin "Delh" is corrected to New Delhi | critical | functional | §5 step 4 (token matching, least obvious rule per §5 note); §5 sanity table `Delh` (base list) |
| TC-13 | TC-13 - origin "Pn" is flagged | critical | functional | §5 step 4/6 (d=2 > cap 1 ⇒ no match); §6 `UNMATCHED_ORIGIN_CITY`; boundary pair with TC-11 |
| TC-14 | TC-14 - origin "Mumbaiii" is flagged | critical | functional | §5 step 4/6 (d=3 > cap 2 ⇒ no match); §6 `UNMATCHED_ORIGIN_CITY`; boundary pair with TC-06 |
| TC-15 | TC-15 - origin "Warsaw" is flagged | high | functional | §5 step 6 (no candidate); §6 `UNMATCHED_ORIGIN_CITY`; §5 sanity table `Warsaw` |
| TC-16 | TC-16 - origin "Lisbon" is flagged | medium | functional | §5 step 6 (no candidate); §6 `UNMATCHED_ORIGIN_CITY` |
| TC-17 | TC-17 - an origin of "" is flagged | high | functional | §5 step 1 (missing — empty); §6 `MISSING_ORIGIN_CITY`; §5 sanity table `""` |
| TC-18 | TC-18 - an origin of "···" is flagged | medium | functional | §5 step 1 (missing — whitespace-only) |
| TC-19 | TC-19 - an absent origin field is flagged | high | functional | §5 step 1 (missing — field absent) |
| TC-20 | TC-20 - a null origin is flagged | medium | functional | §5 step 1 (missing — JSON `null`) |
| TC-76 | TC-76 - origin "Punr" is corrected to Pune | critical | functional | SPEC.md 5 step 4 (confidence floor, accept side). Measured distance 1; paired with TC-77 at the same distance so the pair isolates the floor from the distance cap. |
| TC-77 | TC-77 - origin "Oune" is flagged | critical | functional | SPEC.md 5 step 4 (confidence floor, reject side). The only case in the suite that fails if the floor is lowered or deleted; every other flagged case is rejected by distance. |
| TC-78 | TC-78 - origin "Bangalr" is corrected to Bangalore | high | functional | SPEC.md 5 step 4 (long-name distance cap, accept at the cap). |
| TC-79 | TC-79 - origin "Bangaloreeee" is flagged | high | functional | SPEC.md 5 step 4 (long-name distance cap, reject one edit past it). |
| TC-80 | TC-80 - origin "··New···Delhi·" is corrected to New Delhi | high | functional | SPEC.md 5 step 2 (normalisation: padding and internal runs collapsed) proven through confidence exactly 1.0, which distinguishes the exact path from a fuzzy correction landing on the same city. |
| TC-101 | TC-101 - destination "Mumbai" is corrected to Mumbai | critical | functional | Destination mirror of TC-01: the same clause proven on the other field. §5 step 3 (exact match) |
| TC-102 | TC-102 - destination "MUMBAI" is corrected to Mumbai | critical | functional | Destination mirror of TC-02: the same clause proven on the other field. §5 step 2–3 (case-fold, exact match); §5 sanity table `MUMBAI` |
| TC-103 | TC-103 - destination "mumbai" is corrected to Mumbai | critical | functional | Destination mirror of TC-03: the same clause proven on the other field. §5 step 2–3 (case-fold, exact match) |
| TC-104 | TC-104 - destination "··Mumbai··" is corrected to Mumbai | high | functional | Destination mirror of TC-04: the same clause proven on the other field. §5 step 2 (trim); §5 sanity table `  New   Delhi ` |
| TC-105 | TC-105 - destination "New···Delhi" is corrected to New Delhi | high | functional | Destination mirror of TC-05: the same clause proven on the other field. §5 step 2 (collapse internal whitespace) |
| TC-106 | TC-106 - destination "Mumbi" is corrected to Mumbai | critical | functional | Destination mirror of TC-06: the same clause proven on the other field. §5 step 4 (fuzzy, d=1, name length > 6 ⇒ cap 2); §5 sanity table `Mumbi` |
| TC-107 | TC-107 - destination "now·delhi" is corrected to New Delhi | critical | functional | Destination mirror of TC-07: the same clause proven on the other field. §5 step 4 (token matching via `delhi`); §5 sanity table `now delhi` |
| TC-108 | TC-108 - destination "Bangalor" is corrected to Bangalore | critical | functional | Destination mirror of TC-08: the same clause proven on the other field. §5 step 4 (fuzzy, d=1); §5 sanity table `Bangalor` |
| TC-109 | TC-109 - destination "kolkatta" is corrected to Kolkata | high | functional | Destination mirror of TC-09: the same clause proven on the other field. §5 step 4 (fuzzy, d=1, doubled letter) |
| TC-110 | TC-110 - destination "Ahmedabd" is corrected to Ahmedabad | high | functional | Destination mirror of TC-10: the same clause proven on the other field. §5 step 4 (fuzzy, d=1, deletion) |
| TC-111 | TC-111 - destination "Pne" is corrected to Pune | critical | functional | Destination mirror of TC-11: the same clause proven on the other field. §5 step 4 (short name, length ≤ 6 ⇒ cap 1, JW ≥ 0.85); §5 sanity table `Pne`; boundary pair with TC-13 |
| TC-112 | TC-112 - destination "Delh" is corrected to New Delhi | critical | functional | Destination mirror of TC-12: the same clause proven on the other field. §5 step 4 (token matching, least obvious rule per §5 note); §5 sanity table `Delh` (base list) |
| TC-113 | TC-113 - destination "Pn" is flagged | critical | functional | Destination mirror of TC-13: the same clause proven on the other field. §5 step 4/6 (d=2 > cap 1 ⇒ no match); §6 `UNMATCHED_ORIGIN_CITY`; boundary pair with TC-11 |
| TC-114 | TC-114 - destination "Mumbaiii" is flagged | critical | functional | Destination mirror of TC-14: the same clause proven on the other field. §5 step 4/6 (d=3 > cap 2 ⇒ no match); §6 `UNMATCHED_ORIGIN_CITY`; boundary pair with TC-06 |
| TC-115 | TC-115 - destination "Warsaw" is flagged | high | functional | Destination mirror of TC-15: the same clause proven on the other field. §5 step 6 (no candidate); §6 `UNMATCHED_ORIGIN_CITY`; §5 sanity table `Warsaw` |
| TC-116 | TC-116 - destination "Lisbon" is flagged | medium | functional | Destination mirror of TC-16: the same clause proven on the other field. §5 step 6 (no candidate); §6 `UNMATCHED_ORIGIN_CITY` |
| TC-117 | TC-117 - a destination of "" is flagged | high | functional | Destination mirror of TC-17: the same clause proven on the other field. §5 step 1 (missing — empty); §6 `MISSING_ORIGIN_CITY`; §5 sanity table `""` |
| TC-118 | TC-118 - a destination of "···" is flagged | medium | functional | Destination mirror of TC-18: the same clause proven on the other field. §5 step 1 (missing — whitespace-only) |
| TC-119 | TC-119 - an absent destination field is flagged | high | functional | Destination mirror of TC-19: the same clause proven on the other field. §5 step 1 (missing — field absent) |
| TC-120 | TC-120 - a null destination is flagged | medium | functional | Destination mirror of TC-20: the same clause proven on the other field. §5 step 1 (missing — JSON `null`) |
| TC-176 | TC-176 - destination "Punr" is corrected to Pune | critical | functional | Destination mirror of TC-76: the same clause proven on the other field. SPEC.md 5 step 4 (confidence floor, accept side). Measured distance 1; paired with TC-77 at the same distance so the pair isolates the floor from the distance cap. |
| TC-177 | TC-177 - destination "Oune" is flagged | critical | functional | Destination mirror of TC-77: the same clause proven on the other field. SPEC.md 5 step 4 (confidence floor, reject side). The only case in the suite that fails if the floor is lowered or deleted; every other flagged case is rejected by distance. |
| TC-178 | TC-178 - destination "Bangalr" is corrected to Bangalore | high | functional | Destination mirror of TC-78: the same clause proven on the other field. SPEC.md 5 step 4 (long-name distance cap, accept at the cap). |
| TC-179 | TC-179 - destination "Bangaloreeee" is flagged | high | functional | Destination mirror of TC-79: the same clause proven on the other field. SPEC.md 5 step 4 (long-name distance cap, reject one edit past it). |
| TC-180 | TC-180 - destination "··New···Delhi·" is corrected to New Delhi | high | functional | Destination mirror of TC-80: the same clause proven on the other field. SPEC.md 5 step 2 (normalisation: padding and internal runs collapsed) proven through confidence exactly 1.0, which distinguishes the exact path from a fuzzy correction landing on the same city. |

## edge_cases.feature

6 case(s).

| Case | Scenario | Priority | Type | Derived from |
|---|---|---|---|---|
| TC-70 | A city name with diacritics is preserved intact on the flagged message | high | resilience | §5 step 6 (Kraków is not a reference city ⇒ `UNMATCHED_ORIGIN_CITY`); byte-identical preservation is not itself a SPEC.md clause |
| TC-71 | Non-Latin script survives the round trip without mangling | medium | resilience | As TC-70, for 東京 |
| TC-72 | The requested date passes through byte-identical, with no timezone shift | high | resilience | §4 (`requestedDate` is a raw field carried through unchanged) |
| TC-73 | An invalid date string is passed through, not rejected | medium | resilience | Not specified in SPEC.md; consistent with §4's opaque treatment of `requestedDate` |
| TC-74 | A very long shipper name is handled or rejected predictably | low | resilience | Not specified in SPEC.md — no maximum length is stated for `shipper` |
| TC-75 | A payload near the broker message size limit behaves predictably | low | resilience | Not specified in SPEC.md — broker limits are an infrastructure concern (TEST_SUITE_SPEC.md §13) |

## message_contract.feature

6 case(s).

| Case | Scenario | Priority | Type | Derived from |
|---|---|---|---|---|
| TC-35 | The message key is the booking id on the enriched route | critical | functional | §2 ("Message key: bookingId on all three topics"); mutation table row 4 |
| TC-36 | The message key is the booking id on the flagged route | critical | functional | §2 (message key); mutation table row 4 |
| TC-37 | A publisher correlation-id header survives to the output message | high | functional | Not specified in SPEC.md (header propagation is a suite-level contract expectation, not a §2–§7 clause); mutation table row 5 |
| TC-38 | The service stamps the enrichment status header correctly per route | medium | functional | Not specified in SPEC.md; TEST_SUITE_SPEC.md §7.4 only |
| TC-39 | Bookings sharing a key retain their relative order | high | functional | §7 (`InMemoryMessageBus` / synchronous delivery semantics — ordering is a consequence of the bus contract's per-topic queue) |
| TC-49 | The output message timestamp reflects processing time | medium | functional | TEST_STRATEGY.md 4.1 (message timestamp preserved or deliberately reset, with the choice stated). |

## resilience.feature

7 case(s).

| Case | Scenario | Priority | Type | Derived from |
|---|---|---|---|---|
| TC-50 | A malformed payload is flagged as MALFORMED_MESSAGE | critical | resilience | §7 ("Parse failure → MALFORMED_MESSAGE to booking.flagged. Never throws out of the handler."); §4 (MALFORMED_MESSAGE shape) |
| TC-51 | A valid booking published after a malformed one is still processed | critical | resilience | §7 ("must not stop subsequent deliveries") |
| TC-52 | A malformed message with no key uses the UNKNOWN fallback | medium | resilience | §4 ("bookingId is null and the message key is the Kafka key if present, else UNKNOWN") |
| TC-53 | A duplicate booking id is processed each time it arrives | high | resilience | Out of scope per SPEC.md §2/TEST_SUITE_SPEC.md §13 (exactly-once is explicitly excluded); asserts at-least-once, non-deduplicating behaviour is intentional |
| TC-54 | An empty payload is flagged, not silently dropped | medium | resilience | §7 (never throws; parse failure ⇒ flagged) |
| TC-55 | A JSON array where an object is expected is flagged | low | resilience | §7 (parse failure ⇒ flagged, regardless of shape) |
| TC-56 | A booking is not lost when the consumer restarts mid-batch | high | resilience | TEST_STRATEGY.md 4.1 (offset committed only after successful processing, so a failure mid-processing is a redelivery and not a silent loss). |

## routing_and_flagging.feature

5 case(s).

| Case | Scenario | Priority | Type | Derived from |
|---|---|---|---|---|
| TC-30 | An enriched booking reaches the enriched topic | critical | functional | §2 ("appears on exactly one of booking.enriched / booking.flagged") |
| TC-31 | A flagged booking reaches the flagged topic | critical | functional | §2 (flag routing to a separate topic) |
| TC-32 | A flagged booking never reaches the enriched topic | critical | functional | §2 (exclusivity); mutation table row 3 |
| TC-33 | An enriched booking never reaches the flagged topic | critical | functional | §2 (exclusivity, other direction) |
| TC-34 | Every booking lands on exactly one output topic | critical | functional | §2 (exclusivity, exhaustively); mutation table row 3 |

## schema_contract.feature

8 case(s).

| Case | Scenario | Priority | Type | Derived from |
|---|---|---|---|---|
| TC-40 | Every enriched message validates against the enriched schema | critical | contract | §4 enriched booking shape |
| TC-41 | Every flagged message validates against the flagged schema | critical | contract | §4 flagged booking shape |
| TC-42 | A flagged message fails validation against the enriched schema | high | contract | §2 exclusivity, enforced structurally. There is no `status` enum: CH-09 asked for one and it was deliberately rejected, because SPEC section 4 defines no such field and inventing it would have changed a payload shape the spec forbids changing. Exclusivity comes from `additionalProperties: false` plus disjoint `required` sets, so a flagged payload genuinely fails this schema and an enriched payload genuinely fails the flagged one. See DEF-111. |
| TC-43 | An extra unrecognised field does not break schema conformance | medium | contract | §4. Note the mechanism: the schema sets `additionalProperties: false`, so it does **not** tolerate the extra field. The scenario passes because the service drops unrecognised fields before publishing, and what is asserted is that tolerance on the way in does not become a violation on the way out. |
| TC-44 | Enrichment metadata carries original and corrected values for each city | high | contract | §4 enriched booking shape (`enrichment` block); mutation table row 7 |
| TC-45 | A schema change against the previous version is additive only | medium | contract | Not a SPEC.md clause — exercises `CompatibilityChecker` against the schema file's own git history (TEST_SUITE_SPEC.md §7.5) |
| TC-57 | An added field is accepted as a backward-compatible change | high | contract | TEST_SUITE_SPEC.md 7.4 (schema evolution is additive only). The accepted half of the fixture pair. |
| TC-58 | Removing a required field is rejected as a breaking change | critical | contract | TEST_SUITE_SPEC.md 7.4. The rejected half: a required field removed. Without both halves the comparison could not fail. |

## volume.feature

5 case(s).

| Case | Scenario | Priority | Type | Derived from |
|---|---|---|---|---|
| TC-60 | Every booking is accounted for under load | critical | volume | §8.4 bulk profile (`enriched.size() + flagged.size() == N`) |
| TC-61 | Per-topic totals match the oracle after a bulk run | high | volume | §8.4 (per-topic counts equal the oracle) |
| TC-62 | Per-reason totals match the oracle after a bulk run | high | volume | §8.4 (per-reason counts equal the oracle); §9 (oracle labelled by the real matcher) |
| TC-63 | No booking id appears on both output topics after a bulk run | critical | volume | §8.4 ("no bookingId on both topics"); §2 exclusivity at scale |
| TC-64 | Concurrent publishers lose no messages | high | volume | §7 (`InMemoryMessageBus`/bus contract under concurrent publish — no message-loss guarantee stated explicitly beyond §8.4's aggregate check) |

**Total: 99 traceable case(s).**
